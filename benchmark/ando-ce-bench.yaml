proxy:
  http_addr: "0.0.0.0:9080"
  workers: 0   # auto-detect (one per CPU core)
  keepalive_pool_size: 128

admin:
  addr: "0.0.0.0:9180"
  enabled: true

deployment:
  mode: standalone

observability:
  victoria_metrics:
    enabled: false
  victoria_logs:
    enabled: false
  prometheus:
    enabled: false
